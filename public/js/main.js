//TODO: Import using file import, use file bundler like webpack, and refactor code accordingly

/*
const captureLocalStream      = require('./capture_local_stream');
const establishPeerConnection = require('./peer_connection');

(async () => {
    try {
        await captureLocalStream ();
        await establishPeerConnection ();
    }
    catch (error) {
        console.error (error);
    }
})();
*/